export const API_URL = 'https://kangalos-intern.onrender.com';

// You can add more API-related configuration here if needed
export const API_CONFIG = {
    baseURL: API_URL,
    headers: {
        'Content-Type': 'application/json'
    }
}; 